# RAG Search Module
# Multi-engine fusion search pipeline

# Submodules (auto-discovered by filesystem)
# rag_search  — RagSearchExecutor, RagSearchParams
# pipeline    — RagSearchPipeline, PipelineConfig
# attribution — AttributionBuilder, Attribution, ScoreBreakdown
