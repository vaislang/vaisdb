# RAG Embedding Module
# Embedding model management and reindexing

# Submodules (auto-discovered by filesystem)
# model   — EmbeddingModelInfo, EmbeddingModelRegistry
# manager — EmbeddingManager, ReindexProgress
