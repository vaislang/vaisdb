# Buffer Pool Module
# Memory management for cached database pages
# Includes clock replacement, dirty tracking, partitioning, and read-ahead

# Submodules (auto-discovered by filesystem)
# pool              — Core buffer pool with clock replacement
# frame             — Individual buffer frame with page data
# clock             — Clock replacement algorithm
# dirty_tracker     — Dirty page tracking for checkpoints
# partitioned       — Partitioned buffer pool for reduced contention
# readahead         — Sequential access detection and prefetching
# stats             — Statistics collection and monitoring
