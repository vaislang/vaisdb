# Full-Text Integration Module
# Cross-engine integration with vector and SQL engines

# Submodules (auto-discovered by filesystem)
# fusion        — ScoreFusionMethod (WeightedSum, RRF k=60)
# vector_hybrid — HybridSearchPipeline
# sql           — FullTextRowSource (Volcano adapter)
