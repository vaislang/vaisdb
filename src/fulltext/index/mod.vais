# Full-Text Index Module
# Dictionary, posting lists, compression, and deletion bitmap

# Submodules (auto-discovered by filesystem)
# dictionary      — DictionaryIndex (B+Tree, term hash keys)
# posting         — PostingStore (slotted pages, page chaining)
# compression     — VByte encoding, delta encoding for doc_id lists
# deletion_bitmap — DeletionBitmap (1 bit per doc_id)
