# Full-Text Search Module
# BM25 scoring, phrase search, boolean queries, SQL function

# Submodules (auto-discovered by filesystem)
# bm25     — BM25Scorer (k1=1.2, b=0.75)
# doc_freq — DocFreqTracker
# phrase   — PhraseSearcher (position-based, configurable slop)
# boolean  — BooleanQueryParser/Executor (AND/OR/NOT)
# match_fn — FullTextMatchExecutor (Volcano iterator)
