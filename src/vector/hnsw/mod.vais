# HNSW Index Module
# Hierarchical Navigable Small World graph for approximate nearest neighbor search

# Submodules (auto-discovered by filesystem)
# types    — HNSW configuration, metadata, node/neighbor structs
# insert   — Insert algorithm, NodeStore
# search   — k-NN search, SearchResult
# delete   — Soft delete, MVCC-filtered search, visibility, GC
# layer    — Upper layer management, pinning
# wal      — HNSW WAL record logging
# cow      — Copy-on-write neighbor lists, epoch-based reclamation
# bulk     — Bulk loading for efficient batch construction
