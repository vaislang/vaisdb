# SQL Engine
# Parser, executor, optimizer, transaction manager

# Submodules (auto-discovered by filesystem)
# types, row — Type system and row encoding
# parser — Parser (tokenizer, AST, parser)
# catalog — Catalog (schema metadata: tables, columns, indexes)
