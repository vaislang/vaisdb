# Catalog Module
# Schema metadata management: tables, columns, indexes
# Provides CatalogManager for DDL operations (CREATE TABLE, DROP TABLE, etc.)

# Submodules (auto-discovered by filesystem)
# schema, manager
