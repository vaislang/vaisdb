# Graph Integration Module
# Cross-engine integration with SQL and vector engines

# Submodules (auto-discovered by filesystem)
# sql_join — RowSource adapter for SQL pipeline
# vector   — VectorGraphPipeline (combined scoring)
