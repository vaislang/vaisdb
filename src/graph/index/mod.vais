# Graph Index Module
# B+Tree indexes for label and property lookups

# Submodules (auto-discovered by filesystem)
# label    — LabelIndex (B+Tree, composite key: label_id + node_id)
# property — PropertyIndex (B+Tree, composite key: prop_key_id + value + entity_id)
